<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.order.orderExpireByRedis.mapper.TOrderExpireKeyMapper">


    <insert id="insertExpireKey">
        insert into t_order_expire_key
            (id, order_id, order_no, `redis_key`, status)
        values
            (#{id}, #{orderId}, #{orderNo}, #{redisKey}, #{status})
    </insert>
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.order.order.mapper.OrderMapper">

    <select id="selectExpiredOrders" resultType="com.example.order.order.entity.OrderInfo">
        SELECT *
        FROM order_info
        WHERE status = 0
        AND create_time <![CDATA[<]]> #{expireTime}
        LIMIT 100
    </select>

    <update id="cancelOrder">
        UPDATE order_info
        SET status = 2,
        update_time = NOW()
        WHERE id = #{id}
        AND status = 0
    </update>

    <insert id="insertOrder">
        INSERT INTO order_info(order_no, user_id, status, create_time)
        VALUES (#{orderNo}, #{userId}, 0, NOW())
    </insert>

</mapper>

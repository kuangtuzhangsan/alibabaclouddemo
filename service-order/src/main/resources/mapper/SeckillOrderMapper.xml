<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.order.seckill.mapper.SeckillOrderMapper">

    <insert id="insertSeckillFence" parameterType="map">
        INSERT INTO order_seckill(sku_id, user_id, create_time)
        VALUES (#{skuId}, #{userId}, NOW())
    </insert>

    <insert id="insertOrder" parameterType="map">
        INSERT INTO order_info(order_no, user_id, status, create_time)
        VALUES (#{orderNo}, #{userId}, 0, NOW())
    </insert>

</mapper>